FROM node:22-alpine

WORKDIR /app

# pnpm via corepack (managed by Node)
RUN corepack enable

EXPOSE 5173

# Default command is overridden in docker-compose.yml to run install + dev
CMD ["pnpm", "dev"]
